import type { TocEntry } from "doc-shell/types";

// The current changelog content is symbolic and doesn't define section
// headings/anchors, so it has no sidebar TOC entries yet.
export const changelogToc: TocEntry[] = [];

